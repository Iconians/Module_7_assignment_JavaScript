# Note-Taking App (Browser)

Build a note-taking app with local storage:
- Create, edit, and delete notes
- Save notes to localStorage
- Load notes on page load
- Search/filter notes
- Display note creation date
- Notes persist after page refresh

## Password Generator
Build a password generator with options:
- Set password length
- Include/exclude character types (uppercase, lowercase, numbers, symbols)
- Generate password button
- Copy to clipboard
- Save favorite passwords to localStorage (optional)

**Requirements**
- **Note-Taking App:** Use only concepts from Modules 5–7: DOM, events, forms/dynamic elements, localStorage. Create, edit, delete notes; save/load from localStorage; search or filter; show date. No untaught concepts. Rich text editing is optional.
- **Password Generator:** Form or controls for length and character types; generate and display password; optional copy to clipboard and optional localStorage for favorites. Use only concepts taught in the course so far.
- Notes or favorites persist after refresh. Code runs in a modern browser.

**Submission**
Submit HTML, JS, and optional CSS. The app should run without errors and meet the requirements above.

**Grading Criteria**

| Criterion | Weight |
|-----------|--------|
| Core features (CRUD for notes, or generate + options for password) | 35% |
| localStorage used correctly for persistence | 25% |
| DOM, events, and forms used as taught in Module 7 | 25% |
| Code quality and runnability | 15% |